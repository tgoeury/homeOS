# HomeOS — dépendances Python
# pip install -r requirements.txt

dash>=2.17
plotly>=5.22
requests>=2.31
psutil>=5.9        # métriques système (CPU, RAM, disque, température)

plexapi
python-vlc

paho-mqtt>=1.6     # Subscriber Zigbee2MQTT

yt-dlp>=2024.1     # Téléchargement audio (Data dump)
mutagen>=1.47      # Lecture/écriture tags ID3 et Vorbis

# modules/home_model — modélisation thermique + planification confort
torch>=2.2
pandas>=2.1
numpy>=1.26
matplotlib>=3.8

# À activer au fur et à mesure :
# influxdb-client>=3.0    # Base de données séries temporelles
# APScheduler>=3.10       # Tâches planifiées
# RPi.GPIO>=0.7           # GPIO RPi (sur RPi uniquement)
